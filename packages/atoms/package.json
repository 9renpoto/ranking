{
  "name": "@rate/atoms",
  "version": "0.0.1",
  "esnext": "lib/index.js",
  "files": [
    "lib"
  ],
  "main": "cjs/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -p . --outDir lib && tsc -p . -m commonjs --outDir cjs",
    "clean": "rimraf lib cjs",
    "dev": "tsc -p . -w",
    "prebuild": "yarn clean"
  },
  "typings": "lib/index.d.ts"
}
